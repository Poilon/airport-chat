<div style="max-width: 48rem; margin: 0 auto;">
  <div class="card">
    <div class="card-body">
    <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
      Welcome to Airport Chat! 🌍
    </h2>
    
    <p class="text-gray-600 mb-8 text-center">
      Select an airport to join its chat room and connect with travelers worldwide
    </p>

    <div data-controller="airport-search" class="space-y-4">
      <div class="relative">
        <input 
          type="text" 
          data-airport-search-target="input"
          data-action="input->airport-search#search"
          placeholder="Search for an airport (e.g., Paris, CDG, Tokyo...)"
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
        />
        
        <div 
          data-airport-search-target="results" 
          class="hidden absolute z-10 w-full mt-2 bg-white border border-gray-300 rounded-lg shadow-lg max-h-96 overflow-y-auto"
        >
        </div>
      </div>

      <div class="border-t pt-6 mt-6">
        <h3 class="text-lg font-semibold text-gray-700 mb-4">Popular Airports</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
          <% @airports.limit(10).each do |airport| %>
            <%= link_to room_path(airport.code), class: "block p-4 border border-gray-200 rounded-lg hover:bg-blue-50 hover:border-blue-300 transition" do %>
              <div class="font-semibold text-gray-800"><%= airport.code %> - <%= airport.name %></div>
              <div class="text-sm text-gray-600"><%= airport.city %>, <%= airport.country %></div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
